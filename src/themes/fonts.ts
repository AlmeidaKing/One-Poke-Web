import { Fonts } from './protocols';

const fonts: Fonts = {
  fontFamily: `'Nunito'`,
  fontWeight: {
    light: 300,
    regular: 400,
    semiBold: 600,
    bold: 700,
  },
};

export default fonts;
