import { ThemeBody } from './protocols';

import colors from './colors';
import fonts from './fonts';

const main: ThemeBody = {
  colors: colors,
  fonts: fonts,
};

export default main;
export { main };
